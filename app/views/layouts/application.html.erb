<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

	<title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

	<%= stylesheet_link_tag    "application" %>
	<%= javascript_include_tag "vendor/modernizr" %>
	<%= csrf_meta_tags %>
</head>

<body>

	<div class="row">
    <% if flash[:notice] %>
      <p id="notice"><%= flash[:notice] %></p>
    <% end %>
    <% if flash[:alert] %>
      <p id="alert"><%= flash[:alert] %></p>
    <% end %>
		<div class="large-3 columns">
			<h1><%= link_to( image_tag("http://placehold.it/400x100&text=SEAT YO SELF"), root_path)%></h1>
		</div>
		<div class="large-9 columns">
			<ul class="button-group right">
				<% if current_user %>
					<li><%= link_to 'My Profile', user_path(current_user), {class:"button"}%>
				<% end %>
				<li><a href="/restaurants" class="button">All Restaurants</a></li>
				<li>
						<% if current_user %>
							<%= link_to "Signed in as #{current_user.name}", "#", {class:"button"}%>
						<% else %>
							<%= link_to "Sign Up", new_user_path, {class:"button"}%> 
					  <% end %>
				</li>
				</ul>
			</div>
		</div>






		<div class="row">
			<div class="large-12 columns">
				<%= yield %>
			</div>
		</div>
		<footer class="row">
			<div class="large-12 columns">
				<hr/>
				<div class="row">
					<div class="large-6 columns">
						<p>© Copyright no one at all. Go to town.</p>
					</div>
					<div class="large-6 columns">
						<ul class="inline-list right">
							<li>
								<% if current_user %>
									<%=link_to 'Sign Out', session_path(current_user), method: :delete %>
								<% else %>
									<%=link_to 'Sign In', new_session_path %>
								<% end %>		
							</li>
						</ul>
					</div>
				</div>
			</div>
		</footer>
		<%= javascript_include_tag "application" %>
	</body>
	</html>